import type { Post, ChatMessage } from '../types'

// Community starts clean. Posts are added by real users during the tournament.
export const posts: Post[] = []
export const chatMessages: ChatMessage[] = []
